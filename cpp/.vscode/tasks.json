{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "snowapril",
            "type": "shell",
            "command": "g++",
            "args": [
                "main.cpp", "-g", "-D _TREE_LOG"//, "-O3", "-march=native", "-fPIC",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}