{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "snowapril",
            "type": "shell",
            "command": "g++",
            "args": [
                "-O3", "-march=native", "-fPIC", "main.cpp", "-g"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}